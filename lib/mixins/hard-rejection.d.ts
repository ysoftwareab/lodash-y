/**
 * Part of `lodash-firecloud`.
 *
 * Install the hard-rejection handler.
 * See https://www.npmjs.com/package/hard-rejection.
 *
 * @returns .
 */
export declare let hardRejection: {
    (log?: (stack?: string) => void): void;
    default: any;
};
